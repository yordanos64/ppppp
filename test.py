print("hellow world")
git init                # Only if not already a git repo
git add .               # Stage all changes
git commit -m "message" # Commit your changes
git remote add origin <your-repo-url>  # Only once per project
git push -u origin main # Push to GitHub